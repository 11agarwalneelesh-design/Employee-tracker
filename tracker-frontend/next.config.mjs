/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow your local Wi-Fi IP to connect to development resources like Webpack HMR
  allowedDevOrigins: ['10.107.140.130']
};

export default nextConfig;